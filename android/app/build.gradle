plugins { id 'com.android.application' }

android { namespace 'com.avmirror.local'; compileSdk 35
    defaultConfig { applicationId 'com.avmirror.local'; minSdk 21; targetSdk 35; versionCode 2610; versionName '26.1' }
}

java { toolchain { languageVersion = JavaLanguageVersion.of(17) } }

// O WebView usa o Chromium/WebView fornecido pelo Android. Nenhum navegador
// separado precisa ser instalado pelo usuário.
dependencies { }
